# Your secret key — set this in the host dashboard, NOT in committed code.
GEMINI_API_KEY=your_new_gemini_key_here

# Lock the backend to your frontend origin in production.
# Example: https://yourusername.github.io
ALLOWED_ORIGIN=*

# Optional: port (most hosts set this automatically)
PORT=3000
